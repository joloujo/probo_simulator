# probo_simulator
A simulator for ProbRobo completely from scratch

## Setup

Create a virtual environment and install requirements with `pip install -r requirements.txt`. This project optionally uses `pip-tools` for package management.